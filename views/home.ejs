
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/home.css" />
    <link rel="icon" href="/images/fruits.png" type="image/png">
</head>
<body> 
    <%- include('./includes/navbar', {isLoggedIn: isLoggedIn}) %>  

    <div>
        <div class="pt-3 card-container">
            <% if(products.length > 0){ %>
                <% for( let product of products) { %>
                    <div class="card m-3 custom-card">
                        <img src="/assets/<%= product.image %>" class="card-img-top card-image" alt="<%= product.product_name %>">  
                        <div class="card-body">
                            <div>
                                <div class="name-price-container">
                                    <h3 class="card-title"> <%= product.product_name %> </h3>
                                    <h4 class="card-title fw-light"> <%= product.price %> </h4>
                                </div>
                                <% if(isLoggedIn === true) { %>
                                    <div>
                                        <a href="/editProduct/<%= product.id %>" class="btn btn-outline-dark mx-1 mt-4 edit-button">  
                                            <i class="bi bi-pencil-fill"></i>  
                                        </a>  
                                        <a href="/deleteProduct/<%= product.id %>" class="btn btn-outline-dark mx-1 mt-4 edit-button">
                                            <i class="bi bi-trash-fill "></i>
                                        </a>
                                    </div>
                                 <% } %>
                            </div>
                        </div>
                    </div>
                    <% } %>
                <% } else { %>
                        <div class="m-5 p-5">
                            <h1 class="red">No Products available</h1>
                            <a href="/addProduct" class="btn btn-outline-dark fs-4 my-3">Add products</a>
                        </div>
                    <% } %>
        </div>
    </div>
</body>
</html>
