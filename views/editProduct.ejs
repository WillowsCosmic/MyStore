<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/addProduct.css">
    <link rel="icon" href="/images/fruits.png" type="image/png">  
</head>
<body> 
    <%- include('./includes/navbar') %> 

    <div class="container-fluid mt-4 form-container">
        <form class="w-25 " action="/editProduct/<%=product.id%>" method="post">
            <h2 class="fw-bold fs-3 m-4 mb-2">Edit Product</h2>
            <div class="form-control m-3 p-4">
                <div class="row">
                    <div class="col-8 mt-2 mb-3">
                        <label class="form-label">Product Name</label>
                        <input type="text" name="product_name" value="<%= product.product_name %>" class="form-control"/>
                    </div>
                    <div class="col-4 mt-2 mb-3">
                        <label class="form-label">Price</label>
                        <input type="text" name="price" value="<%= product.price %>" class="form-control"/>
                    </div>
                </div>
                <div>
                    <label class="form-label">Select Image</label>
                    <input type="file" name="image" value="<%= product.image %>" class="form-control"/>
                </div>
            </div>
            <button class="btn btn-primary fs-4 m-3 w-100">Save Changes</button>
        </form>
    </div>
</body>
</html>